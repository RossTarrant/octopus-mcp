# Example environment variables
# Copy this to .env and fill in your values

# GitHub Personal Access Token — required for the list_discussions tool.
# Create one at https://github.com/settings/tokens (needs repo/public_repo scope).
# GITHUB_TOKEN=your-token-here

# Server configuration
PORT=3000
